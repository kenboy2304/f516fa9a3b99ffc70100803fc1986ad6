@model List<string>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

@foreach (var item in Model)
{
    <p>
        <a href="@Url.Action("Index","BookManager",new {version = item})">@item</a>
    </p>
}